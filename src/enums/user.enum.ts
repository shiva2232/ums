/*********************************************
 * #### Hint
 * * not all possible values were got from api so add enum with a pipe string to resolve runtime problem of dynamic data (In case of exception).
 ********************************************/
export enum Gender {
  male = 'male',
  female = 'female',
  other = 'other',
}

/*********************************************
 * #### Hint
 * * not all possible values were got from api so add enum with a pipe string to resolve runtime problem of dynamic data (In case of exception).
 ********************************************/
export enum UserTitle {
  Mr = 'Mr',
  Ms = 'Ms',
  Mrs = 'Mrs',
  Dr = 'Dr',
  Sir = 'Sir',
}
